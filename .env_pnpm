# pnpm requires PNPM_HOME to be set and on PATH to know where to install global
# binaries. Without it, `pnpm install -g` fails with ERR_PNPM_NO_GLOBAL_BIN_DIR.
export PNPM_HOME="$HOME/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
